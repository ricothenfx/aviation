# Decision Log

| Field | Value |
|---|---|
| Status | Binding, append-only |
| Created | 2026-09-22 |
| Governance | New decisions get a new ID here + (if architectural) an ADR in `adr/`. Superseding requires a new entry pointing at the old one. Never edit a decided entry. |

## Decisions

| ID | Date | Decision | Alternatives rejected | Consequence |
|---|---|---|---|---|
| D-01 | 2026-09-22 | Portfolio track = **senior Full-Stack TypeScript/Next.js**. Seniority must show through architecture depth: ADRs, trade-off narratives, production-hardening (observability, failure handling), not through feature count. | AI/ML-first track; pure frontend track | 1 deep primary JD target (CAG Req 7075). AI appears as a layer inside full-stack systems, never as a standalone notebook. |
| D-02 | 2026-09-22 | Build order locked: **turnaround-iq → mro-copilot → rebook-ai**. turnaround-iq has the widest employer coverage and maps 1:1 to CAG Req 7075. | mro-copilot first (AI depth) | No project starts before its predecessor reaches F5 (unless user says so). |
| D-03 | 2026-09-22 | Stack locked: TypeScript + Next.js + Tailwind (UI), PostgreSQL + TimescaleDB (relational + time-series), Redis (live state/pub-sub), Python FastAPI only for AI-serving services, Docker Compose (local), AWS-native service mapping + IaC-ready config, GitHub Actions CI, Playwright e2e. Versions locked in tech-stack.md. | MongoDB (CAG stack — see ADR-0002); Node-only AI (ecosystem too weak for eval/ML); Kubernetes (ops overhead unjustified for portfolio) | Agent may not introduce any library/category outside tech-stack.md without an approved ADR. |
| D-04 | 2026-09-22 | turnaround-iq core = **event-driven with event sourcing** for the ground-task log (see ADR-0001). All state changes arrive as immutable events from the simulator; read models are projections. | CRUD + polling | Enables replayable disruption scenarios, audit trail, honest real-time claims. Read-model complexity must be handled, documented in code. |
| D-05 | 2026-09-22 | Build executed **by coding agents in milestone increments** with hard quality gates (DoD per milestone in milestones.md; CI non-negotiable; tests cannot be skipped/vacuous). | Continuous unstructured agent sessions | User reviews each milestone; agents must not jump ahead of the assigned milestone. |
| D-06 | 2026-09-22 | **Public repository documents in English** (README, PRD, ADR, code, commit messages) because Singapore reviewers may read them. Live conversation with the user may be Indonesian. | Indonesian docs | All docs in this repo are written in English. |
| D-07 | 2026-09-22 | **All data synthetic/simulated, visibly labeled in the UI.** Allowed datasets only: NASA C-MAPSS, OpenSky Network (research terms), OurAirports, hand-written scenarios. No real-company affiliation claims, logos, PNRs. | Scraping real ops data; unbranded realism | Every screen carries a "Simulated data" disclaimer. data-ethics.md is binding. |
| D-08 | 2026-09-22 | The 3 projects live in **one monorepo sharing a design system, event-contract conventions, CI, and infra patterns** to cut total build time (see ADR-0004). | 3 separate repos | packages/ shared code must stay generic; no cross-import of project-specific domain logic. |
| D-09 | 2026-09-22 | AuthN in local dev = seeded users + JWT sessions through a provider abstraction whose AWS target is **Cognito**; RBAC roles per project (turnaround-iq: coordinator / supervisor / viewer). | Full Cognito from day one (blocks local dev); no auth (fails JD requirement 8) | Auth module must keep the Cognito swap behind one interface; traceability-matrix row 8 depends on it. |
| D-10 | 2026-09-22 | LLM integration must go through a **provider-agnostic gateway abstraction** with local mock mode (see ADR-0003), so demos work offline and cost-zero while remaining Bedrock-compatible in shape. | Direct SDK calls to a single vendor | Copilot features degrade gracefully to rule-based engines when the LLM layer is off. |
| D-11 | 2026-09-23 | turnaround-iq F3: replan-engine deploys as a **sixth compose worker service** (risk daemon tails PG event_log; replan RPC via `chan:replan:control` + result keys; approved plans applied as `task.rescheduled` events). Replan lifecycle events `replan.approved`/`replan.rejected` join the contracts **additively** (ws frames included). Confirmed with the user in-session; details in ADR-0005. | Rule engine inside the web container (blurs architecture.md §2 boundaries); HTTP RPC service (extra plumbing for one command) | Decisions read PostgreSQL only; same seed + log ⇒ identical plan hash is CI-tested; one more container in CI. |

| D-12 | 2026-09-24 | F5 live demo deploys to a **single small VPS running the same six-service compose topology behind a production override (prod images, loopback-only data services), fronted by Caddy for auto-TLS**. AWS stays as the locked deploy-shape mapping (tech-stack.md §2); `infra/deploy/` provides IaC-ready structure. Details and rejected alternatives in ADR-0006. | Live AWS deploy (contradicts locked tech-stack §2; ws redesign; ≈ 3–5× cost); Fly.io (platform-specific wiring); Cloudflare Tunnel (dies with dev machine) | Public demo ≈ US$6/mo; deployment artifacts themselves evidence JD rows 7/10; revisit live AWS only via a new ADR. |
| D-13 | 2026-09-24 | Grafana + Prometheus enter the stack as an **ephemeral `evidence` compose profile** used only for F5 load runs (k6 remote-writes to Prometheus; dashboard JSON + k6 script committed for reproducibility). Never part of the demo stack. First exception recorded against tech-stack.md §3 — details in ADR-0007. | k6 text-only output (milestone scope is binding); permanent observability profile; host-native installs | Milestone-prescribed dashboard exists reproducibly; ~100 MB one-time image pull; metrics are per-run by design. |
| D-14 | 2026-09-24 | F5 load findings drive three gateway hardening fixes plus **one additive wire frame type `board.batch`** (gateway emits one batch envelope per client flush instead of ~12 individual frames; clients unwrap inner frames whose per-frame causality/latency semantics are unchanged). Fixes: (1) pub/sub subscriptions stacked across reset cycles — Nth reset processed N times; (2) per-event PG projection writes (13 round-trips/event) saturated the pool and stretched rebuilds to minutes — now coalesced per flight (500 ms) with bulk task UPDATE; (3) per-frame ws sends ~24k syscalls/s overran the flush budget. Evidence and before/after numbers in ADR-0008 + load-report-f5.md. | Coalescing event-history frames (degrades audit feed); binary protocol (outside locked stack); reducing test scale (dishonest per D-07) | DoD gate (p95 ingestion→board < 1 s at ×10/200 consumers) becomes reachable; wire change is additive and wire-only (never enters the event log). |
| D-15 | 2026-09-24 | turnaround-iq post-F5 polish review (supervisor board first-view): **(a)** the PRD F-5 "weather hold" script is **deferred**, not silently dropped or built — the disruption catalog stays at the three F3 scripts; **(b)** `onTimeDepPct`/`avgTurnMin` become **nullable until the first off-block turn** (additive contract change; 0%/0-min would present "no data" as a bad measurement, PRD F-6 honesty). Details in ADR-0009. | Building weather hold without a milestone (scope discipline); treating 0.0% as a valid idle reading (dishonest KPI); editing the PRD without a decision record | Scenario console renders 3 injection buttons by decision; board idle state shows "—" on both tiles; contract consumers updated additively. |
| D-16 | 2026-09-24 | mro-copilot vector store = **pgvector extension in the existing PostgreSQL 16 cluster** (HNSW cosine index + hybrid lexical/semantic retrieval via RRF); no dedicated vector DB. Details in ADR-0010. | Dedicated vector DB (Qdrant/Weaviate — new infra outside locked stack for a sub-1k-chunk corpus); in-process FAISS index files (index/chunk lifecycle drift, no SQL metadata filters) | One engine + one migration tool; re-ingest transactional; scale ceiling (~10M chunks) documented as limitation, revisit via new ADR. |
| D-17 | 2026-09-24 | mro-copilot RUL model = **scikit-learn `HistGradientBoostingRegressor` on windowed degradation features with piecewise-linear RUL target (clip 125, FD001)**, trained only via committed seeded CLI producing versioned artifacts (`joblib` + `metrics.json` + sha256). Details in ADR-0011. | PyTorch LSTM (literature standard, few RMSE points better — gap must be reported honestly per D-07); survival analysis (no censored labels in C-MAPSS) | Deterministic CPU-only training/serving; RMSE + NASA score reported next to published baselines; deep model revisit requires new ADR. |
| D-18 | 2026-09-24 | mro-copilot AI topology = **one Python `ai-service` (FastAPI) owning embeddings + retrieval + RUL with a Python port of the ADR-0003 gateway (deterministic 384-dim hashed-n-gram mock embedder); RAG orchestration, guardrails, sign-off stay TS in the Next.js app via `packages/llm-gateway`**; degrade-to-extractive answers labeled `source: "extractive"`. Details in ADR-0012. | RAG fully in Python (splits product domain logic out of the TS application layer); all AI in TS (contradicts D-03/tech-stack.md); embeddings in TS + retrieval in Python (two AI runtimes in one pipeline) | Interface parity between TS and Python gateways enforced by contract tests; ai-service internal-only (bearer `AI_SERVICE_TOKEN`); vector-space consistency by construction. |
| D-19 | 2026-09-26 | mro-copilot F5 load finding: the F2 single-connection retrieval serialization caps search/ask throughput, making the F5 gate (search p95 < 300 ms @ 100 VU) structurally unreachable. **`RetrievalService` moves to a small in-module bounded connection pool (psycopg stdlib only, capacity 8, transient overflow connections, no new package)**; RulService unchanged. Details, alternatives, and re-measured numbers in ADR-0013 + load-report-f5.md. | Keep single connection and report the gate as failed (dishonest per D-07 when the fix is small and local); psycopg_pool package (new dependency for no gain at capacity 8); per-request connections (discards F2 reuse win); async psycopg rewrite (oversized blast radius) | F5 gates re-measured with before/after evidence; up to 8 idle connections per process; retrieval SQL and public interface unchanged. |

## Supersession History

- (empty — no decision has been superseded yet)
