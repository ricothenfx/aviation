# Copy to .env ON THE SERVER (infra/deploy/mro/.env) — never commit the
# filled-in file. Generate secrets with: openssl rand -hex 32
DOMAIN=mro-copilot.aviation.ricothen.com
ACME_EMAIL=you@example.com
AUTH_SECRET=change-me-openssl-rand-hex-32
POSTGRES_PASSWORD=change-me-openssl-rand-hex-32
AI_SERVICE_TOKEN=change-me-openssl-rand-hex-32
