{
  "name": "@aviation/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared event envelope, API error/pagination and payload schemas (api-contracts.md).",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@aviation/config": "workspace:*",
    "@types/node": "^22.10.5",
    "tsup": "^8.3.5",
    "typescript": "^5.6.3",
    "vitest": "^3.0.5"
  }
}
