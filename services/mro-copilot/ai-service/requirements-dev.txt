# Dev tooling for lint / typecheck / tests (CI: ruff + mypy + pytest).
# Not installed in the serving container.
-r requirements.txt
httpx==0.28.1
pytest==8.3.5
pytest-asyncio==0.25.3
mypy==1.15.0
ruff==0.11.9
